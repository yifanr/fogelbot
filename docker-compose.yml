version: "3.8"

services:
  fogelbot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fogelbot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      FACT_DB_PATH: /data/fogelbot.db
    volumes:
      - ./fogelbot.db:/data/fogelbot.db
